# Database setup

For a clean installation, create an empty `servicedesk` database and import `schema.sql`.

`schema.sql` is the single source of truth for the current database structure. Versioned Node migration scripts are intentionally not used in v0.6.

`seed.sql` is optional and contains commented examples only.
